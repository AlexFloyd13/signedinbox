# SignedInbox Environment Variables
# Copy to .env.local and fill in your values

# Supabase (required)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# Encryption key for Ed25519 private keys (required — generate with: openssl rand -hex 32)
ENCRYPTION_KEY=your-32-byte-hex-key

# API key HMAC secret (required — generate with: openssl rand -hex 32)
API_KEY_HASH_SECRET=your-hmac-secret

# Cloudflare Turnstile (required in production)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key
TURNSTILE_SECRET_KEY=your-turnstile-secret-key
# Dev only — never set in Vercel production
# TURNSTILE_DEV_BYPASS=true

# Site URL
NEXT_PUBLIC_SITE_URL=https://signedinbox.com

# CORS — comma-separated extra origins (optional)
# CORS_ALLOWED_ORIGINS=https://myapp.com
